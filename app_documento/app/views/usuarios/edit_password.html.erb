<%= semantic_form_for @usuario, :remote => true, :url => update_password_usuarios_path do |f| %>
  <ul>
    <table class="table_forms" style="border: none;">
      <tbody>
        <tr>
          <td>
            <%= f.input :oldpassword, :label => "Contraseña actual"%>
          </td>
        </tr>
        <tr>
          <td>
            <%= f.input :password, :label => "Contraseña nueva"%>
          </td>
        </tr>
        <tr>
          <td>
            <%= f.input :password_confirmation, :label => "Confirmar contraseña"%>
          </td>
        </tr>
        <tr>
          <td>
            <%= f.action :submit, :label => "Cambiar contraseña" %>
          </td>
        </tr>
      </tbody>
    </table>
  </ul>
<% end %>

